name: "CodeQL Analysis"

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  skip-analysis:
    name: Skip CodeQL Analysis
    runs-on: ubuntu-latest
    
    steps:
    - name: Skip CodeQL for stencil repository
      run: |
        echo "⏭️ Skipping CodeQL analysis for stencil repository"
        echo "✅ This repository contains only stencil files, no source code to analyze"
        echo "🎨 Repository validated successfully"