name: CodeQL

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    if: false  # Disabled - no Python code in stencil repository
    
    steps:
    - name: Skip CodeQL
      run: echo "CodeQL disabled for stencil repository"