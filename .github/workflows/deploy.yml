name: Stencil Validation

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v5
      
      - name: Validate stencil collection
        run: |
          echo "🎨 Validating VMware Visio Stencils Collection..."
          
          # Check ZIP file
          if [ -f "VMware-Stencils-2024.zip" ]; then
            echo "✅ Found VMware-Stencils-2024.zip ($(du -h VMware-Stencils-2024.zip | cut -f1))"
          else
            echo "❌ Missing VMware-Stencils-2024.zip"
            exit 1
          fi
          
          # Validate directories
          for dir in stencils icons omnigraffle; do
            if [ -d "$dir" ]; then
              count=$(find "$dir" -type f | wc -l)
              echo "✅ $dir/ directory: $count files"
            else
              echo "❌ Missing $dir/ directory"
              exit 1
            fi
          done
          
          # Check specific file types
          echo "📊 File breakdown:"
          echo "  Visio files (.vssx/.vstm): $(find stencils/ -name '*.vssx' -o -name '*.vstm' | wc -l)"
          echo "  SVG icons: $(find icons/ -name '*.svg' | wc -l)"
          echo "  OmniGraffle files: $(find omnigraffle/ -name '*.gstencil' -o -name '*.clr' | wc -l)"
          
          echo "🎉 Stencil collection validation completed successfully!"
