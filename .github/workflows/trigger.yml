name: Trigger Actions User
on:
  push:
    branches: [ main ]
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Trigger actions-user
      run: echo 'Triggering actions-user contributor'
